---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: sonarqube 
    tag: latest

inputs:
  - name: source-code2

outputs:
  - name: build-output2

run:
  path: source-code2/ci/build.sh
