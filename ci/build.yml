---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: sonarqube 
    tag: latest

inputs:
  - name: source-code1

outputs:
  - name: build-output1

run:
  path: source-code1/ci/build.sh
